"""Shared fixtures for integration tests."""

import pytest


# Example fixture — replace with real setup
@pytest.fixture
def sample_data_dir(tmp_path):
    """Provides a temporary directory pre-populated with test data."""
    return tmp_path
